# Nixpacks configuration for Railway
# Run worker directly, no npm wrapper

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = []

[start]
cmd = "./node_modules/.bin/tsx workers/sms-worker.ts"

