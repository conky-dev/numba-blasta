# Railway Configuration for SMS Worker

[build]
builder = "NIXPACKS"
nixpacksConfigPath = ".nixpacks.toml"
watchPatterns = ["workers/**"]

[deploy]
startCommand = "./node_modules/.bin/tsx workers/sms-worker.ts"
restartPolicyType = "NEVER"

