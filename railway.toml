# Railway Configuration for SMS Worker

[build]
builder = "NIXPACKS"
nixpacksConfigPath = ".nixpacks.toml"
watchPatterns = ["workers/**"]

[deploy]
startCommand = "echo 'Worker disabled' && exit 0"
restartPolicyType = "NEVER"

